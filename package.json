{
  "name": "pokemon-code-index",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute pokemon-code-index --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute pokemon-code-index --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}